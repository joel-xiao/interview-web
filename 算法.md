
---

# **一、数组与字符串**

### 1️⃣ 两数之和（Two Sum）

**题目**：给定数组 `nums` 和目标值 `target`，返回和为 target 的两个下标。

```js
function twoSum(nums, target) {
  const map = new Map();
  for(let i=0; i<nums.length; i++){
    const complement = target - nums[i];
    if(map.has(complement)) return [map.get(complement), i];
    map.set(nums[i], i);
  }
}
```

**思路**：用哈希表存已访问元素 → O(n) 时间复杂度

---

### 2️⃣ 数组去重

```js
// ES6 Set
const unique = arr => [...new Set(arr)];

// 手写
function unique(arr) {
  const res = [];
  const map = {};
  for(let i of arr){
    if(!map[i]){
      res.push(i);
      map[i] = true;
    }
  }
  return res;
}
```

---

### 3️⃣ 翻转字符串 / 字符串反转

```js
function reverseStr(str) {
  return str.split('').reverse().join('');
}
```

* 手写循环反转也可：

```js
function reverseStr(str) {
  let res = '';
  for(let i=str.length-1;i>=0;i--){
    res += str[i];
  }
  return res;
}
```

---

# **二、链表**

### 1️⃣ 反转链表

```js
function reverseList(head) {
  let prev = null, curr = head;
  while(curr){
    const next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
  }
  return prev;
}
```

---

### 2️⃣ 合并两个有序链表

```js
function mergeTwoLists(l1, l2) {
  const dummy = new ListNode(0);
  let curr = dummy;
  while(l1 && l2){
    if(l1.val < l2.val){
      curr.next = l1;
      l1 = l1.next;
    } else {
      curr.next = l2;
      l2 = l2.next;
    }
    curr = curr.next;
  }
  curr.next = l1 || l2;
  return dummy.next;
}
```

---

# **三、递归 & 树**

### 1️⃣ 求数组全排列（递归）

```js
function permute(arr) {
  const res = [];
  const dfs = (path, used) => {
    if(path.length === arr.length){
      res.push([...path]);
      return;
    }
    for(let i=0;i<arr.length;i++){
      if(used[i]) continue;
      used[i] = true;
      path.push(arr[i]);
      dfs(path, used);
      path.pop();
      used[i] = false;
    }
  }
  dfs([], []);
  return res;
}
```

---

### 2️⃣ 二叉树前序遍历

```js
function preorder(root) {
  if(!root) return [];
  return [root.val, ...preorder(root.left), ...preorder(root.right)];
}
```

* 中序：`[...inorder(left), root.val, ...inorder(right)]`
* 后序：`[...postorder(left), ...postorder(right), root.val]`

---

# **四、排序与搜索**

### 1️⃣ 冒泡排序

```js
function bubbleSort(arr) {
  for(let i=0;i<arr.length;i++){
    for(let j=0;j<arr.length-1-i;j++){
      if(arr[j] > arr[j+1]){
        [arr[j], arr[j+1]] = [arr[j+1], arr[j]];
      }
    }
  }
  return arr;
}
```

### 2️⃣ 二分查找

```js
function binarySearch(arr, target) {
  let left=0, right=arr.length-1;
  while(left<=right){
    const mid = Math.floor((left+right)/2);
    if(arr[mid] === target) return mid;
    if(arr[mid] < target) left = mid + 1;
    else right = mid - 1;
  }
  return -1;
}
```

---

# **六、栈与队列**

### 1️⃣ 有效括号匹配（栈）

```js
function isValid(s) {
  const stack = [];
  const map = { '(':')', '[':']', '{':'}' };
  for(let c of s){
    if(map[c]){
      stack.push(c);
    } else {
      if(c !== map[stack.pop()]) return false;
    }
  }
  return stack.length === 0;
}
```

* 栈用于保存未匹配的左括号
* 面试重点：stack.push / stack.pop / map 对应关系

---

### 2️⃣ 最小栈

```js
class MinStack {
  constructor() {
    this.stack = [];
    this.minStack = [];
  }
  push(val) {
    this.stack.push(val);
    if(!this.minStack.length || val <= this.minStack[this.minStack.length-1]) {
      this.minStack.push(val);
    }
  }
  pop() {
    const val = this.stack.pop();
    if(val === this.minStack[this.minStack.length-1]) this.minStack.pop();
    return val;
  }
  top() { return this.stack[this.stack.length-1]; }
  getMin() { return this.minStack[this.minStack.length-1]; }
}
```

---

### 3️⃣ 队列实现栈 / 栈实现队列

* 面试题：用两个队列实现栈，或用两个栈实现队列

---

# **七、滑动窗口**

### 1️⃣ 最大子数组和长度 k

```js
function maxSumK(arr, k) {
  let sum = 0, max = -Infinity;
  for(let i=0;i<k;i++) sum += arr[i];
  max = sum;
  for(let i=k;i<arr.length;i++){
    sum = sum - arr[i-k] + arr[i];
    max = Math.max(max, sum);
  }
  return max;
}
```

### 2️⃣ 无重复字符的最长子串

```js
function lengthOfLongestSubstring(s) {
  const set = new Set();
  let left=0, maxLen=0;
  for(let right=0; right<s.length; right++){
    while(set.has(s[right])){
      set.delete(s[left++]);
    }
    set.add(s[right]);
    maxLen = Math.max(maxLen, right-left+1);
  }
  return maxLen;
}
```

---

# **八、动态规划（DP）**

### 1️⃣ 爬楼梯

```js
function climbStairs(n) {
  if(n<=2) return n;
  let a=1, b=2;
  for(let i=3;i<=n;i++){
    [a,b] = [b,a+b];
  }
  return b;
}
```

### 2️⃣ 最大子序和

```js
function maxSubArray(nums) {
  let maxSum = nums[0], currSum = nums[0];
  for(let i=1;i<nums.length;i++){
    currSum = Math.max(nums[i], currSum+nums[i]);
    maxSum = Math.max(maxSum, currSum);
  }
  return maxSum;
}
```

### 3️⃣ 编辑距离（Levenshtein Distance）

```js
function minDistance(word1, word2) {
  const m = word1.length, n = word2.length;
  const dp = Array.from({length:m+1}, () => Array(n+1).fill(0));
  for(let i=0;i<=m;i++) dp[i][0] = i;
  for(let j=0;j<=n;j++) dp[0][j] = j;

  for(let i=1;i<=m;i++){
    for(let j=1;j<=n;j++){
      if(word1[i-1]===word2[j-1]) dp[i][j]=dp[i-1][j-1];
      else dp[i][j]=1+Math.min(dp[i-1][j], dp[i][j-1], dp[i-1][j-1]);
    }
  }
  return dp[m][n];
}
```

---

# **九、贪心算法**

### 1️⃣ 分发饼干 / 匹配问题

* 给孩子饼干，使尽量多的孩子满足
* 贪心策略：先满足胃口小的孩子

```js
function findContentChildren(g, s) {
  g.sort((a, b) => a - b);
  s.sort((a, b) => a - b);

  let i = 0; // 指向孩子
  let j = 0; // 指向饼干
  let count = 0;

  while (i < g.length && j < s.length) {
    if (s[j] >= g[i]) {
      // 饼干满足孩子
      count++;
      i++;
      j++;
    } else {
      // 饼干太小，换更大的
      j++;
    }
  }

  return count;
}
```
```
```

### 2️⃣ 会议室问题

* 给定会议时间，计算最少会议室数量
* 贪心策略：按开始时间排序 → 使用最早结束的会议室

```js
function minMeetingRooms(intervals) {
  const starts = intervals.map(i => i[0]).sort((a,b)=>a-b);
  const ends = intervals.map(i => i[1]).sort((a,b)=>a-b);

  let rooms = 0;
  let endPtr = 0;

  for (let i = 0; i < starts.length; i++) {
    if (starts[i] < ends[endPtr]) {
      rooms++; // 需要新会议室
    } else {
      endPtr++; // 复用会议室
    }
  }

  return rooms;
}
```
```
```
---

# **十、字符串匹配与滑动窗口结合**

### 1️⃣ 最小覆盖子串

* 给定字符串 s 和 t，找到 s 中包含 t 所有字符的最短子串
* 使用滑动窗口 + 哈希表

```js
function minWindow(s, t) {
  const need = new Map();
  for(let c of t) need.set(c, (need.get(c)||0)+1);
  let left=0,right=0,count=t.length,minLen=Infinity,start=0;

  while(right<s.length){
    const c = s[right];
    if(need.has(c) && need.get(c)>0) count--;
    if(need.has(c)) need.set(c, need.get(c)-1);
    right++;
    while(count===0){
      if(right-left<minLen){ minLen = right-left; start = left; }
      const d = s[left];
      if(need.has(d)) need.set(d, need.get(d)+1);
      if(need.has(d) && need.get(d)>0) count++;
      left++;
    }
  }
  return minLen===Infinity ? '' : s.substr(start, minLen);
}
```

---

💡 **复习总结**

1. **数组 & 字符串**：两数之和、翻转、去重、滑动窗口
2. **链表**：反转、合并、环检测
3. **树 & 递归**：前/中/后序遍历、全排列、子集
4. **栈 & 队列**：括号匹配、最小栈、实现栈队列
5. **动态规划**：爬楼梯、最大子序和、编辑距离
6. **贪心 & 双指针**：分发问题、会议室、最小覆盖子串

---

我可以帮你画一张 **算法题思维导图**，把 **数组、字符串、链表、树、栈、队列、滑动窗口、动态规划、贪心** 全部分类整理，并标注手写模板/核心思路，让面试快速复述。

你希望我画吗？

