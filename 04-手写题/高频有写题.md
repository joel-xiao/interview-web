# IT面试手写高频题（全场景覆盖版）
涵盖**异步并发/定时器、数组/字符串、闭包/防抖节流、DOM/BOM、简单算法/逻辑、对象/函数进阶、网络/工程化**七大核心场景，均为前端/后端通用（JS/Node.js）的面试必写题，难度从基础到进阶梯度分布，贴合你无刷题基础、练核心逻辑的需求，仅给出问题，无考点和提示，可按之前的「拆题→套模板→填代码」思路练习。

## 一、异步并发/定时器（你的核心熟悉区，补充变式题）
1. 实现3次串行请求，一次执行完再执行下一次，全部完成后打印「所有请求执行完毕」
 ```js
const count = 3;
 function request () {
  for (let i= 0; i< count; i ++) {
   await  fetch(url)
  }
 } 
```
```
```
2. 实现最多2次并发的不间断请求，完成一个立即补一个，持续执行
```js
 const count = 2;
 const requests = new Array(10).fill(2);
 function request() {
   const req = requests.pop();
   requests.unshift(req);
   return req;
 }

 function start() {
   const task = [];
    for (let i=0; i<count; i++) {
      task.push(Promise.resolve(request()));
   }

   return Promise.all(task)
 }

```
```
```
3. 实现5次并发请求，单个请求失败自动重试1次，全部完成后返回成功/失败结果数组

```js
const count = 5;
let idx = 0;
const requests = [1,2,3,3,4,5,6]

function getReq() {
  return requests[idx++ % requests.length];
}


async function sendReq(){
  let prev;
  while(true) {
    const curr = prev || getReq();
    const pr = await prev;
    if (pr.err) {
      if (!prev) {
        prev = curr;
      } else {
        prev = null;
      }
    } else {
      prev = null;
    }
  }
}
function start() {
    for(let i = 0; i < count; i++) {
      sendReq();
    }
}
```
4. 实现每3秒发起一次5次并发请求，非不间断，固定间隔轮询
```js

const count =5;
const requests = new Array(5).fill(1)
let idx = 0;
let time = new Date().getTime()
function start() {
  requestAnmaitionFrame(async () => {
    if (time + 3000 < new Date.getTime()) {
      const task = []
      for(let i =0; i< count; i++) {
        task.push(Promise.resolve(getReq()));
      }

      await Promise.all(task);
      time = new Date.getTime()
    }
  })
}

function getReq() {
 return requests[idx++ % requests.length];
}
```
```
```
5. 实现带终止条件的不间断并发请求：累计执行10轮5次并发后，自动停止并打印「执行结束」
```js
const count = 10;
const req_count = 5;
function start() {
  for (let f = 0; f < req_count; f++) {
    await loop();
  }
}

const requests =  new Array(5).fill(1);
function getReq() {
  return requests[idx++ % requests.length];
}

function loop() {
  const map = new Map();
  let is = true;
  while(is) {
    if (map.get(curr) === count) is = false return;
    const curr = getReq();
    map.set(curr, (map.get(curr) || 0) + 1);;
    await Promise.resolve()
  }
}

```
6. 实现请求重试逻辑：单个请求最多重试3次，3次均失败则抛出错误，不再执行
```js

```js
const count = 5;
let idx = 0;
const requests = [1,2,3,3,4,5,6]

function getReq() {
  return requests[idx++ % requests.length];
}


async function sendReq(){
  let is = true;
  let prev;
  let err = 0;
  while(is) {
    const curr = prev || getReq();
    const pr = await prev;
    if (pr.err) {
      prev = curr;
      err++;
    } else {
      err = 0;
      prev = null;
    }

    if (err === 3){
      is = false;
      console.error(prev)
    }
  }
}
function start() {
  sendReq();
}
```
7. 用Promise实现串行执行一个请求数组，按数组顺序依次执行，全部完成后返回结果
```js

Promise.propertype.MyAll = async function(tasks)  {
  const result = []
  for(let item in tasks) {
    result.push(await item())
  }
  return result
}
```

## 二、数组/字符串操作（面试最基础高频，必练）
1. 实现数组去重，兼容基本数据类型（数字、字符串、布尔值）
```js
  new Set([1,'2', true])

  //2 

  const map = new Map();
  for(let item in arr) {
    if(!map.has(item)){
      item.set(item)
    }
  }
  Array.from(map.keys())

  let left =0, right = arr.length -1;
  arr = arr.sort()
  const newArr = []
  while( left < right) {
   if () 
  }
```
2. 实现一维数组转二维数组（分块），入参为原数组和每块长度，例：[1,2,3,4,5]，分块长度2 → [[1,2],[3,4],[5]]
```js
  const count = 2;
  const newarr = []
  while(arr.length) {
    newarr.push(arr.splice(0, count))
  }
  let left = 0, right = 2;
  while(right < arr.length) {
    left +2;
    right +2;
    newarr.push([arr[left], arr[right]].filter(Boolean))
  }
```
```
```
3. 实现数组扁平化，将多维数组转为一维数组（例：[1,[2,[3,4]]] → [1,2,3,4]）
```js

  arr.flat();

  const newarr = [];
  for (let item in arr) {
    newarr.push( Array.isArray(item) ? (...item) : item)
  }
4. 实现字符串反转，例：'abcdef' → 'fedcba'
  a.split('').reverse().join('')
  
  a.split('').reduce((prev, curr) => curr + prev, '');
```

5. 统计字符串中每个字符出现的次数，返回键值对对象（例：'abac' → {a:2, b:1, c:1}）
```js
const job = {};
 for(let item in str.split('')) {
  job[item] = job[item] ? job[item] + 1 : 1;
}
```
6. 实现数组排序（冒泡/选择排序任选），对数字数组进行升序排列
```js
  for(let i = 0; i < arr.length; i++) {
  for(let j = i + 1; j < arr.length; j++) {
   if (arr[i] > arr[j]) {
    [arr[i], arr[j]] = [arr[j], arr[i]
}
  }
}
```
7. 从数组中找出第一个重复的元素，例：[1,2,3,2,1] → 2
```js
 const a = {};
 for (let item in arr) {
   if (a[item]) return item;
   a[item] = true;
 }
 ```
8. 实现截取字符串的前n个字符，超出部分用「...」代替（例：'hello world'，n=5 → 'hello...'）
```js
 str.substring(0, n) + ( n < str.length ? '...' : '...'
```
9. 合并两个有序数字数组为一个新的有序数组，不修改原数组（例：[1,3,5] + [2,4,6] → [1,2,3,4,5,6]）
```js
 const a = [...a1,...a2].sort()

 const concat = [];
 let n1 = 0, n2 = 0;
 while (n1 < a1.length && n2 < a2.length) {
   if (n1 === a1.length) {
      concat.push(a2[n2]);
     n2++;
   } else if (n2 === a2.length) {
      concat.push(a1[n1]);
     n1++;
   } else if (a1[n1] < a2[n2]) {
     cancat.push(a1[n1]);
     n1++;
   } else {
     cancat.push(a2[n2]);
     n2++;
   }
 }

 const concat = new Array(a1.length + a2.length).fill(0);
 let left =0;
 let right = concat.length -1;

 let
 while() {

 }
```
10. 实现数组的filter方法，手动封装一个myFilter，功能与原生filter一致
```js
 Array.prototype.myFilter = function (cb) {
    const a = [];
    for( let i=0; i< this.length; i++) {
      if (cb(item,i, this)) {
        a.push(item);
     }
   }

   return a;
 }
```

## 三、闭包/作用域/防抖节流（面试必考，核心闭包应用）
1. 手写防抖函数（debounce），触发事件后延迟n秒执行，期间再次触发则重置延迟时间
```js
 function debounce(fn, delay) {
   let timer;
  return function (..args) {
     clearTimeout(timer)
     timer = setTimeout(() => fn(...args), delay)
   }
 }
```
2. 手写节流函数（throttle），触发事件后n秒内只执行一次，高频触发时按固定频率执行
```js
function throttle(fn, delay) {
  let ts = new Date().getTime()
  return function(...args) {
    if (ts + delay < new Date().getTime()) {
      fn(...args)
      ts = new Date().getTime()
    }
  }
}
```
3. 实现一个计数器，用闭包实现每次调用自增1并返回当前值
```js
 function count(n) {
   let a = n;
    return function () {
      a = a + 1;
     return a;
   }
 }
```
4. 解释并解决for循环绑定事件的经典问题（用闭包/let/定时器传参任选方式）
```js
const btn = Array.from(document.querySelectorAll('button'));
 for (let i =0; i< btn.length; i++) { 
    btn.i = i;
      btn.onclick = function (event) {
        console.log(event.i)
    }
 }
```
5. 实现一个闭包函数，缓存函数的执行结果（简单缓存，入参为基本类型）
```js
  function cahce(fn) {
    const map = new Map();
    return function (..args) {
      if (map.has(args.join('-')) return map.get(args);
      map.set(args.join('-'), fn(...args));
      return map.get(args.join('-'));

    }
  }
```
6. 用闭包实现一个私有变量，只能通过暴露的方法获取和修改，不能直接访问
```js

function  count(n) {
  let count = n;
   function add(n) {
     count += n;
   }
   function get() {
     return count;
   }
   return {
     add,
     get
   }
}

const a = count(2);
a.add(1)
a.get();
```
## 四、简单DOM/BOM操作（前端通用基础，偏工程实操）
1. 实现一个函数，动态创建一个DOM元素并添加到页面body中，可设置元素的类名、内容

function appendBodyChild(tag, className, content) {
  const el = document.createElement(tag);
  el.interHTML = content;
  el.className = className;
  document.body.appendChild(el);
}
2. 实现一个函数，获取指定DOM元素的所有父级元素，返回数组
```js
function getParent(targent) {
  const els = tyeof targent === 'string' ? Array.from(document.querySelectorAll(targent)) ? [target];

  const parents = [];
  for (let item of els) {
    parents.push(item.patchElemet);
  }
  // const parents = [];
  // for (let item of  els) {
  // while(item.parentNode) {
  //   parents.push(item.parentNode);
  //   item = item.parentNode;
  //   }
  // }
}
```
3. 实现元素的拖拽功能，让页面中一个div可随鼠标拖动
```js
const el = document.querySelector('.box');
let el_rect = {}
const startPos = { x: 0, y: 0}
el.onmousedown = function (event) { 
startPos.x = event.clientX;
startPos.y = event.clientY;
 el_rect = el.getBoundingClientRect();
 document.body.onmousemove = onMouseMove;
}

el.onmouseup = function() {
document.body.onmousemove = null;
}

function onMouseMove(event) {
el.style.left = rect.left + event.clientX - startPos.x + 'px';
el.style.top = rect.top + event.clientX - startPos.y + 'px';
}
```
4. 实现获取浏览器视口的宽高，兼容PC和移动端
5. 实现一个简单的倒计时功能，在页面中实时显示剩余时分秒（例：距离指定时间还有01:02:03）
6. 实现禁止页面的右键菜单和文字选中
7. 实现一个函数，判断一个元素是否在另一个元素的内部（包含子元素）

## 五、简单算法/逻辑题（低难度，不考复杂数据结构，练思路）
1. 实现二分查找，在有序数字数组中查找目标值，找到返回索引，未找到返回-1
```js
function find (nums, n){
  let left = 0, right = nums.length;
  while (left < right) {
    if (nums[left] === n) return left;
    if (nums[right] === n) return right;
    left++;
    right--;
  }
  return -1;
}

function find (nums, n){
  let left = 0, left_2 = 2;
  if (nums[0] === n) return left;
  if (nums[1] === n) return 1;
  if (nums[2] === n) return 2;

  while (left_2 < nums.length) {
    if (nums[left] === n) return left;
    if (nums[left_2] === n) return left_2;
    left++;
    left_2++;
  }
  return -1;
}
```
2. 实现一个函数，判断一个数字是否为质数（素数）
3. 实现斐波那契数列，输入n，返回第n项的值（1,1,2,3,5,8...）
```js
function (n) {
  let sum = 1;
  let sum_2 = 1;
  if (n === 1 || n === 2) return 1;
  while (n > 2) {
    n--;
    let curr = sum;
    sum = sum + sum_2;
    sum_2 = curr
  }
}
```
4. 实现一个函数，判断一个字符串是否为回文字符串（正读和反读一致，例：'level' → 是，'abc' → 否）
```js
function is(str) {
  if (typeof str !== 'string') return false;
  let i = 0;
  let j = str.length - 1;
  while (j > i) {
    if (str[i++] !== str[j--]) return false;
  }
  return true;
}
```
5. 计算一个数组中所有数字的和，要求用递归实现
```js
function a(arr) {
  if (!Array.isArray(arr)) return undifined;
  if (arr.length === 0) return 0;
   return arr[0] + a(arr.slice(1));
}
```
6. 实现一个函数，将数字转为千分位格式，例：1234567 → '1,234,567'
```js
function split() {
 const str = String(num).split('');
 const newstr = [];
  let i = 0;
  while(str.length) {
    i = i++;
    newstr.unshift(str.pop());
    if (i == 2) {
      i = 0;
    newstr.unshift(',');
    }
  }
  return newstr.join('');
}

function replace(num) {
  const str = String(num);
  return str.replace(/(?=(\d{3})+$))/g, ',').replace(/^,/, '')
}
```
7. 实现石头剪刀布游戏，随机生成电脑出拳，对比玩家出拳并返回胜负结果
```js
const arr = ['拳头','剪刀', '布'];
const rules = {
  '拳头': '剪刀',
  '剪刀': '布',
  '布': '拳头'
}
function a( str) {
  if (arr.indexOf(str) === -1) return '输入有误';

  const computer = arr[Math.floor(Math.random() * arr.length)];
  return {
    user: str,
    computer: computer,
    is: computer === str ? '平局' : rules[str] === computer ? '胜利' : '失败'
  }
}
```
## 六、对象/函数进阶（原型/深拷贝/柯里化，高频进阶）
1. 实现深拷贝，兼容对象、数组、基本数据类型（不考虑循环引用）
```js
// 暴力解法
function cloneDeep(obj) {
 return JSON.parse(JSON.stringify(obj)) 
}

function cloneDeep(obj) {
  // const map = new Map();
  if (typeof obj !== 'object' || obj === null) return obj;
    // if (map.has(obj)) return map.get(obj);
    // map.set(obj, newObj);

  const newObj = Array.sisArray(obj) ? [] : {};
  for (let key in obj) {
    newObj[key] = cloneDeep(obj[key])
  }
  return newObj;
}
```
2. 实现浅拷贝，手动封装一个myClone，功能与Object.assign一致
```js
Object.prototype.myClone = function (obj, ...args) {
    if (tyeof obj !== 'object' || obj === null) return obj;
    const newObj = Array.isArray(obj) ? [] : {};
    for (let obj of args) { 
      if (tyeof obj !== 'object' || obj === null) return 'error';
      for(let key in obj) {
        if(obj.hasOwnProperty(key)) (newObj[key] = obj[key]);
      }
    }

    return newObj;
  }
```
3. 手写函数柯里化，将多参数函数转为单参数依次调用的形式（例：add(1,2,3) → add(1)(2)(3)）
```js
function add(num) {
  // 修复1：初始参数容错（无参数时count初始化为0）
  let count = num ?? 0;

  function b(num) {
    // 修复2：无参数调用时，直接返回最终数值（终止递归）
    if (num === undefined) return count;
    count += num;
    return b;
  }

  // 修复3：重写ES6标准的Symbol.toPrimitive，替代toString/valueOf，让函数直接表现为数值
  b[Symbol.toPrimitive] = () => count;
  // 兼容低版本环境，保留toString/valueOf
  b.toString = b.valueOf = () => count;

  return b;
}
```
4. 实现一个构造函数，并用原型添加方法，实例可调用该方法
5. 实现Object.create的功能，手动封装一个myCreate，创建一个以指定对象为原型的新对象
6. 实现一个函数，合并多个对象为一个新对象，后面的对象覆盖前面的属性，不修改原对象
7. 手写数组的map方法，手动封装一个myMap，功能与原生map一致
8. 实现一个函数，判断一个变量是否为纯对象（排除数组、null、函数）

## 七、基础异步封装/网络（Promise/请求封装，衔接并发题）
1. 用Promise封装一个sleep函数，入参为毫秒数，await sleep(1000)可暂停1秒
```js
function sleep(time) {
  return new Promise(resolve => setTimeout(resolve, time));
}
```
2. 用Promise封装一个简易版fetch/ajax，模拟请求成功/失败，支持then/catch
```js

```
3. 实现带超时控制的请求函数，请求超过3秒未完成则判定为超时失败
4. 手动实现简易版Promise.all，入参为Promise数组，等待所有完成后返回结果数组，一个失败则整体失败
5. 手动实现简易版Promise.race，入参为Promise数组，返回第一个完成的Promise的结果（成功/失败）
6. 实现一个请求池，限制最大并发数为3，传入一个请求数组，按规则执行所有请求
7. 用Promise实现一个简易的异步任务队列，依次执行任务，前一个任务完成后再执行下一个
