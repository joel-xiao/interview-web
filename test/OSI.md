七层模型

应用层 https http ftp dns websoket

表示层  编解码 二进制 加解密 tls ssl  等

会话层  如 soket 长链接  token 会话

传输层  TCP（一般用来 接口请求） UDP（一般用来视频播放 或者 3D 留） 等

网络层 比如 IP寻址 源 ip 到 目标 ip

链路层 如 MAC， wifi 局域网 交换机

物理层 如 光纤 电缆，信号 


四层模型

应用层 https http dns ftp soket

传输层 tcp UDP

网络层 IP Mac

链路层 网络 光纤


